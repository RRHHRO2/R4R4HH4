@model IEnumerable<Proyecto.Models.TipoDocumento>

@{
    ViewBag.Title = "Listado de Tipos de Documento";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid" data-aos="fade-up" data-aos-duration="800">
    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-header bg-gradient-primary text-white rounded-top-4 d-flex justify-content-between align-items-center">
            <h5 class="mb-0 fw-semibold">
                <i class="fas fa-id-card me-2"></i>Listado de Tipos de Documento
            </h5>
            <a href="@Url.Action("Create")" class="btn btn-light btn-sm shadow-sm rounded-pill px-3" data-bs-toggle="tooltip" title="Agregar nuevo tipo">
                <i class="fas fa-plus me-1"></i> Nuevo
            </a>
        </div>
        <div class="card-body bg-light rounded-bottom-4">
            <div class="table-responsive">
                <table class="table table-hover table-striped align-middle shadow-sm rounded">
                    <thead class="table-dark text-center">
                        <tr>
                            <th>Nombre</th>
                            <th>Abreviatura</th>
                            <th class="text-center">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr class="align-middle">
                                <td>@item.Nombre</td>
                                <td>@item.Abreviatura</td>
                                <td class="text-center">
                                    <a href="@Url.Action("Edit", new { id = item.IdTipoDocumento })" class="btn btn-outline-warning btn-sm rounded-pill mx-1" data-bs-toggle="tooltip" title="Editar">
                                        <i class="fas fa-pen"></i>
                                    </a>
                                    <a href="@Url.Action("Details", new { id = item.IdTipoDocumento })" class="btn btn-outline-info btn-sm text-white rounded-pill mx-1" data-bs-toggle="tooltip" title="Detalles">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <a href="@Url.Action("Delete", new { id = item.IdTipoDocumento })" class="btn btn-outline-danger btn-sm rounded-pill mx-1" data-bs-toggle="tooltip" title="Eliminar">
                                        <i class="fas fa-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Inicializar tooltips Bootstrap
        var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
        tooltipTriggerList.map(function (tooltipTriggerEl) {
            return new bootstrap.Tooltip(tooltipTriggerEl);
        });
    </script>
}
